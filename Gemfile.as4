source "https://rubygems.org"

# Gemspec only lists command-line tool dependencies
gemspec

# Restrict to Rails 4 for older Rubies
gem 'activesupport', '~> 4.0'

# Not-required for execution
group :development, :test do
  gem 'rspec', '~> 2.6.0'
  gem 'rake'
  gem 'redis'
end